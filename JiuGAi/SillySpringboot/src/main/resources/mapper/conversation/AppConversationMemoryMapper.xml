<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sillyspringboot.conversation.mapper.AppConversationMemoryMapper">

    <resultMap id="MemMap" type="com.example.sillyspringboot.conversation.entity.AppConversationMemory">
        <id column="conversation_id" property="conversationId"/>
        <result column="branch_id" property="branchId"/>
        <result column="summary_preview" property="summaryPreview"/>
        <result column="facts_count" property="factsCount"/>
        <result column="memory_world_name" property="memoryWorldName"/>
        <result column="entry_count" property="entryCount"/>
        <result column="enabled_entry_count" property="enabledEntryCount"/>
        <result column="manual_revision" property="manualRevision"/>
        <result column="memory_revision" property="memoryRevision"/>
        <result column="applied_source_revision" property="appliedSourceRevision"/>
        <result column="last_source_message_id" property="lastSourceMessageId"/>
        <result column="last_refreshed_message_count" property="lastRefreshedMessageCount"/>
        <result column="last_manual_refresh_at" property="lastManualRefreshAt"/>
        <result column="manual_refresh_started_at" property="manualRefreshStartedAt"/>
        <result column="manual_refresh_token" property="manualRefreshToken"/>
        <result column="last_synced_at" property="lastSyncedAt"/>
        <result column="sync_status" property="syncStatus"/>
        <result column="sync_error" property="syncError"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByConversationId" resultMap="MemMap">
        SELECT conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
               entry_count, enabled_entry_count, manual_revision, memory_revision, applied_source_revision, last_source_message_id,
               last_refreshed_message_count, last_manual_refresh_at,
               manual_refresh_started_at, manual_refresh_token, last_synced_at,
               sync_status, sync_error, updated_at
        FROM app_conversation_memory
        WHERE conversation_id = #{conversationId}
        ORDER BY updated_at DESC
        LIMIT 1
    </select>

    <select id="findByConversationBranchId" resultMap="MemMap">
        SELECT conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
               entry_count, enabled_entry_count, manual_revision, memory_revision, applied_source_revision, last_source_message_id,
               last_refreshed_message_count, last_manual_refresh_at,
               manual_refresh_started_at, manual_refresh_token, last_synced_at,
               sync_status, sync_error, updated_at
        FROM app_conversation_memory
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
        LIMIT 1
    </select>

    <select id="findByConversationBranchIdForUpdate" resultMap="MemMap">
        SELECT conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
               entry_count, enabled_entry_count, manual_revision, memory_revision, applied_source_revision, last_source_message_id,
               last_refreshed_message_count, last_manual_refresh_at,
               manual_refresh_started_at, manual_refresh_token, last_synced_at,
               sync_status, sync_error, updated_at
        FROM app_conversation_memory
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="listByConversationId" resultMap="MemMap">
        SELECT conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
               entry_count, enabled_entry_count, manual_revision, memory_revision, applied_source_revision, last_source_message_id,
               last_refreshed_message_count, last_manual_refresh_at,
               manual_refresh_started_at, manual_refresh_token, last_synced_at,
               sync_status, sync_error, updated_at
        FROM app_conversation_memory
        WHERE conversation_id = #{conversationId}
        ORDER BY branch_id ASC
    </select>

    <insert id="upsertTouch">
        INSERT INTO app_conversation_memory (conversation_id, branch_id, summary_preview, facts_count, updated_at)
        VALUES (#{conversationId}, 0, NULL, 0, CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="upsertTouchForBranch">
        INSERT INTO app_conversation_memory (conversation_id, branch_id, summary_preview, facts_count, updated_at)
        VALUES (#{conversationId}, #{branchId}, NULL, 0, CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="ensureForBranch">
        INSERT IGNORE INTO app_conversation_memory (
            conversation_id, branch_id, summary_preview, facts_count, updated_at
        ) VALUES (
            #{conversationId}, #{branchId}, NULL, 0, CURRENT_TIMESTAMP
        )
    </insert>

    <insert id="upsertRollup">
        INSERT INTO app_conversation_memory (conversation_id, branch_id, summary_preview, facts_count, updated_at)
        VALUES (#{conversationId}, 0, #{summaryPreview}, #{factsCount}, CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE
            summary_preview = VALUES(summary_preview),
            facts_count = VALUES(facts_count),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="upsertRollupForBranch">
        INSERT INTO app_conversation_memory (conversation_id, branch_id, summary_preview, facts_count, updated_at)
        VALUES (#{conversationId}, #{branchId}, #{summaryPreview}, #{factsCount}, CURRENT_TIMESTAMP)
        ON DUPLICATE KEY UPDATE
            summary_preview = VALUES(summary_preview),
            facts_count = VALUES(facts_count),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="upsertRefreshState">
        INSERT INTO app_conversation_memory (
            conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
            entry_count, enabled_entry_count, last_source_message_id,
            last_refreshed_message_count, sync_status, sync_error, updated_at
        ) VALUES (
            #{conversationId}, 0, #{summaryPreview}, #{factsCount}, #{memoryWorldName},
            #{entryCount}, #{enabledEntryCount}, #{lastSourceMessageId},
            #{lastRefreshedMessageCount}, #{syncStatus}, #{syncError}, CURRENT_TIMESTAMP
        )
        ON DUPLICATE KEY UPDATE
            summary_preview = VALUES(summary_preview),
            facts_count = VALUES(facts_count),
            memory_world_name = VALUES(memory_world_name),
            entry_count = VALUES(entry_count),
            enabled_entry_count = VALUES(enabled_entry_count),
            last_source_message_id = VALUES(last_source_message_id),
            last_refreshed_message_count = VALUES(last_refreshed_message_count),
            sync_status = VALUES(sync_status),
            sync_error = VALUES(sync_error),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <insert id="upsertRefreshStateForBranch">
        INSERT INTO app_conversation_memory (
            conversation_id, branch_id, summary_preview, facts_count, memory_world_name,
            entry_count, enabled_entry_count, last_source_message_id,
            last_refreshed_message_count, sync_status, sync_error, updated_at
        ) VALUES (
            #{conversationId}, #{branchId}, #{summaryPreview}, #{factsCount}, #{memoryWorldName},
            #{entryCount}, #{enabledEntryCount}, #{lastSourceMessageId},
            #{lastRefreshedMessageCount}, #{syncStatus}, #{syncError}, CURRENT_TIMESTAMP
        )
        ON DUPLICATE KEY UPDATE
            summary_preview = VALUES(summary_preview),
            facts_count = VALUES(facts_count),
            memory_world_name = VALUES(memory_world_name),
            entry_count = VALUES(entry_count),
            enabled_entry_count = VALUES(enabled_entry_count),
            last_source_message_id = VALUES(last_source_message_id),
            last_refreshed_message_count = VALUES(last_refreshed_message_count),
            sync_status = VALUES(sync_status),
            sync_error = VALUES(sync_error),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <update id="updateSyncStatus">
        UPDATE app_conversation_memory
        SET memory_world_name = #{memoryWorldName},
            entry_count = #{entryCount},
            enabled_entry_count = #{enabledEntryCount},
            sync_status = #{syncStatus},
            sync_error = #{syncError},
            last_synced_at = CASE WHEN #{syncStatus} = 'SUCCESS' THEN CURRENT_TIMESTAMP ELSE last_synced_at END,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
    </update>

    <update id="updateSyncStatusForBranch">
        UPDATE app_conversation_memory
        SET memory_world_name = #{memoryWorldName},
            entry_count = #{entryCount},
            enabled_entry_count = #{enabledEntryCount},
            sync_status = #{syncStatus},
            sync_error = #{syncError},
            last_synced_at = CASE WHEN #{syncStatus} = 'SUCCESS' THEN CURRENT_TIMESTAMP ELSE last_synced_at END,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
    </update>

    <update id="tryAcquireManualRefresh">
        UPDATE app_conversation_memory
        SET last_manual_refresh_at = CURRENT_TIMESTAMP,
            manual_refresh_started_at = CURRENT_TIMESTAMP,
            manual_refresh_token = #{token},
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
          AND (manual_refresh_token IS NULL
               OR manual_refresh_started_at IS NULL
               OR manual_refresh_started_at &lt;= #{leaseCutoff})
          AND (last_manual_refresh_at IS NULL
               OR last_manual_refresh_at &lt;= #{cooldownCutoff})
    </update>

    <update id="releaseManualRefresh">
        UPDATE app_conversation_memory
        SET manual_refresh_started_at = NULL,
            manual_refresh_token = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
          AND manual_refresh_token = #{token}
    </update>

    <update id="updateRefreshStateWithRevision">
        UPDATE app_conversation_memory
        SET summary_preview = #{summaryPreview},
            facts_count = #{factsCount},
            entry_count = #{entryCount},
            enabled_entry_count = #{enabledEntryCount},
            last_source_message_id = #{lastSourceMessageId},
            last_refreshed_message_count = #{lastRefreshedMessageCount},
            sync_status = #{syncStatus},
            sync_error = NULL,
            applied_source_revision = #{appliedSourceRevision},
            memory_revision = memory_revision + 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
          AND manual_revision = #{expectedManualRevision}
          AND memory_revision = #{expectedMemoryRevision}
    </update>

    <update id="updateAfterManualMutation">
        UPDATE app_conversation_memory
        SET summary_preview = #{summaryPreview},
            facts_count = #{factsCount},
            entry_count = #{entryCount},
            enabled_entry_count = #{enabledEntryCount},
            sync_status = #{syncStatus},
            sync_error = NULL,
            manual_revision = manual_revision + 1,
            memory_revision = memory_revision + 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
          AND manual_revision = #{expectedManualRevision}
          AND memory_revision = #{expectedMemoryRevision}
    </update>

    <update id="updateAfterHistoryInvalidation">
        UPDATE app_conversation_memory
        SET summary_preview = #{summaryPreview},
            facts_count = #{factsCount},
            entry_count = #{entryCount},
            enabled_entry_count = #{enabledEntryCount},
            last_source_message_id = NULL,
            last_refreshed_message_count = 0,
            sync_status = #{syncStatus},
            sync_error = NULL,
            applied_source_revision = 0,
            memory_revision = memory_revision + 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE conversation_id = #{conversationId}
          AND branch_id = #{branchId}
          AND memory_revision = #{expectedMemoryRevision}
    </update>

    <delete id="deleteByConversationId">
        DELETE FROM app_conversation_memory
        WHERE conversation_id = #{conversationId}
    </delete>
</mapper>
