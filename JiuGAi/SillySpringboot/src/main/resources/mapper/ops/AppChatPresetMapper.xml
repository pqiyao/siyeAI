<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sillyspringboot.ops.mapper.AppChatPresetMapper">

    <resultMap id="AppChatPresetResultMap" type="com.example.sillyspringboot.ops.entity.AppChatPreset">
        <id column="id" property="id"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="scope" property="scope"/>
        <result column="source_type" property="sourceType"/>
        <result column="api_type" property="apiType"/>
        <result column="source_name" property="sourceName"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="bundle_json" property="bundleJson"/>
        <result column="enabled" property="enabled"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="last_synced_at" property="lastSyncedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, owner_user_id, scope, source_type, api_type, source_name, name, description,
        bundle_json, enabled, sort_order, last_synced_at, created_at, updated_at
    </sql>

    <select id="findById" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findEnabledPublicById" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE id = #{id}
          AND scope = 'PUBLIC'
          AND enabled = 1
        LIMIT 1
    </select>

    <select id="findEnabledAvailableById" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE id = #{id}
          AND enabled = 1
          AND (
              (scope = 'PUBLIC' AND owner_user_id IS NULL)
              OR (scope = 'PRIVATE' AND owner_user_id = #{userId})
          )
        LIMIT 1
    </select>

    <select id="findPrivateByIdForOwner" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE id = #{id}
          AND scope = 'PRIVATE'
          AND owner_user_id = #{userId}
        LIMIT 1
    </select>

    <select id="listPublicEnabled" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE scope = 'PUBLIC'
          AND enabled = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="listPrivateByOwner" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        WHERE scope = 'PRIVATE'
          AND owner_user_id = #{userId}
        ORDER BY updated_at DESC, id DESC
    </select>

    <insert id="insertPrivate" parameterType="com.example.sillyspringboot.ops.entity.AppChatPreset"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_chat_preset (
            owner_user_id, scope, source_type, api_type, source_name, name, description,
            bundle_json, enabled, sort_order
        ) VALUES (
            #{ownerUserId}, 'PRIVATE', 'USER_COPY', 'openai', #{sourceName}, #{name}, #{description},
            #{bundleJson}, #{enabled}, 100
        )
    </insert>

    <update id="updatePrivate">
        UPDATE app_chat_preset
        SET name = #{name},
            description = #{description},
            bundle_json = #{bundleJson},
            enabled = #{enabled},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND scope = 'PRIVATE'
          AND owner_user_id = #{userId}
    </update>

    <delete id="deletePrivate">
        DELETE FROM app_chat_preset
        WHERE id = #{id}
          AND scope = 'PRIVATE'
          AND owner_user_id = #{userId}
    </delete>

    <sql id="AdminWhere">
        WHERE scope = 'PUBLIC'
        <if test="keyword != null and keyword != ''">
            AND (
                name LIKE CONCAT('%', #{keyword}, '%')
                OR source_name LIKE CONCAT('%', #{keyword}, '%')
                OR description LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        <if test="apiType != null and apiType != ''">
            AND api_type = #{apiType}
        </if>
        <if test="enabled != null">
            AND enabled = #{enabled}
        </if>
    </sql>

    <select id="listAdmin" resultMap="AppChatPresetResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM app_chat_preset
        <include refid="AdminWhere"/>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAdmin" resultType="long">
        SELECT COUNT(*)
        FROM app_chat_preset
        <include refid="AdminWhere"/>
    </select>

    <insert id="upsertPlatformPreset" parameterType="com.example.sillyspringboot.ops.entity.AppChatPreset"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_chat_preset (
            owner_user_id, scope, source_type, api_type, source_name, name, description,
            bundle_json, enabled, sort_order, last_synced_at
        ) VALUES (
            #{ownerUserId}, #{scope}, #{sourceType}, #{apiType}, #{sourceName}, #{name}, #{description},
            #{bundleJson}, #{enabled}, #{sortOrder}, #{lastSyncedAt}
        )
        ON DUPLICATE KEY UPDATE
            name = VALUES(name),
            description = VALUES(description),
            bundle_json = VALUES(bundle_json),
            last_synced_at = VALUES(last_synced_at),
            updated_at = CURRENT_TIMESTAMP
    </insert>

    <update id="updateStatus">
        UPDATE app_chat_preset
        SET enabled = #{enabled},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND scope = 'PUBLIC'
    </update>

    <update id="updateSortOrder">
        UPDATE app_chat_preset
        SET sort_order = #{sortOrder},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND scope = 'PUBLIC'
    </update>

    <delete id="deleteById">
        DELETE FROM app_chat_preset
        WHERE id = #{id}
          AND scope = 'PUBLIC'
    </delete>
</mapper>
