<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sillyspringboot.auth.mapper.AppPasswordResetTokenMapper">

    <resultMap id="PasswordResetTokenResultMap" type="com.example.sillyspringboot.auth.entity.AppPasswordResetToken">
        <id column="id" property="id"/>
        <result column="request_id" property="requestId"/>
        <result column="user_id" property="userId"/>
        <result column="account_key" property="accountKey"/>
        <result column="code_hash" property="codeHash"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="consumed_at" property="consumedAt"/>
        <result column="attempt_count" property="attemptCount"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <resultMap id="PasswordResetThrottleResultMap" type="com.example.sillyspringboot.auth.entity.AppPasswordResetThrottle">
        <id column="account_key" property="accountKey"/>
        <result column="request_id" property="requestId"/>
        <result column="requested_at" property="requestedAt"/>
    </resultMap>

    <select id="findByRequestIdForUpdate" resultMap="PasswordResetTokenResultMap">
        SELECT id, request_id, user_id, account_key, code_hash, expires_at, consumed_at, attempt_count, created_at
        FROM app_password_reset_token
        WHERE request_id = #{requestId}
        LIMIT 1
        FOR UPDATE
    </select>

    <insert id="insertThrottleIfAbsent">
        INSERT IGNORE INTO app_password_reset_throttle (account_key, request_id)
        VALUES (#{accountKey}, #{requestId})
    </insert>

    <select id="findThrottleForUpdate" resultMap="PasswordResetThrottleResultMap">
        SELECT account_key, request_id, requested_at
        FROM app_password_reset_throttle
        WHERE account_key = #{accountKey}
        FOR UPDATE
    </select>

    <update id="updateThrottle">
        UPDATE app_password_reset_throttle
        SET request_id = #{requestId},
            requested_at = CURRENT_TIMESTAMP
        WHERE account_key = #{accountKey}
    </update>

    <delete id="deleteThrottleBefore">
        DELETE FROM app_password_reset_throttle
        WHERE requested_at &lt; #{cutoff}
    </delete>

    <insert id="insert" parameterType="com.example.sillyspringboot.auth.entity.AppPasswordResetToken" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO app_password_reset_token (request_id, user_id, account_key, code_hash, expires_at)
        VALUES (#{requestId}, #{userId}, #{accountKey}, #{codeHash}, #{expiresAt})
    </insert>

    <update id="invalidateActiveByUserId">
        UPDATE app_password_reset_token
        SET consumed_at = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
          AND consumed_at IS NULL
    </update>

    <update id="incrementAttemptCount">
        UPDATE app_password_reset_token
        SET attempt_count = attempt_count + 1,
            consumed_at = CASE WHEN attempt_count + 1 &gt;= #{maxAttempts} THEN CURRENT_TIMESTAMP ELSE consumed_at END
        WHERE id = #{id}
          AND consumed_at IS NULL
    </update>

    <update id="markConsumed">
        UPDATE app_password_reset_token
        SET consumed_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
          AND consumed_at IS NULL
    </update>

    <delete id="deleteExpiredBefore">
        DELETE FROM app_password_reset_token
        WHERE expires_at &lt; #{cutoff}
    </delete>
</mapper>
